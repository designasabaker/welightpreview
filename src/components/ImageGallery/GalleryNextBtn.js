export const GalleryNextBtn = (props) => {
    const {onClick} = props;

    return(
        <button
            onClick={onClick}
        >
            Next
        </button>
    )
}

export default GalleryNextBtn;